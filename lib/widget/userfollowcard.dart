import 'package:flutter/material.dart';

class UserFollowCard extends StatelessWidget {
  const UserFollowCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
